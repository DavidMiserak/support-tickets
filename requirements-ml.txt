# requirements-ml.txt
#
# Optional dependencies for the ML worker backends, both off by default:
#   - SUMMARIZER_BACKEND=transformer  (DistilBART summarizer)
#   - CLASSIFIER_BACKEND=zeroshot     (zero-shot priority/spam/routing)
# Not installed in the default API or worker container images; install locally
# for ML development or extend the worker image (see README).
#
# First run downloads model weights to the HuggingFace cache: ~300 MB for
# DistilBART, plus ~700 MB for the zero-shot MNLI model (downloaded separately,
# only when CLASSIFIER_BACKEND=zeroshot).
#
# CPU-only PyTorch wheels (worker uses device=-1). Drop the extra-index line
# if you install a CUDA build intentionally.
--extra-index-url https://download.pytorch.org/whl/cpu
-r requirements.txt

torch==2.10.0
transformers==4.57.6
