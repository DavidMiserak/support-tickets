# requirements-dev.txt
#
# Test-only dependencies. Installed in the Docker `test` stage and for local
# test runs; NOT shipped in the runtime image.
-r requirements.txt

# Real-Postgres-backed tests. testcontainers spins up postgres:16 when
# TEST_DATABASE_URL is not provided; psycopg2 powers its readiness probe.
testcontainers[postgres]==4.14.2
psycopg2-binary==2.9.12
